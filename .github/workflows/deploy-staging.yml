# Deploy staging to VPS on push to `staging`.
#
# Required repository secrets (Settings → Secrets and variables → Actions):
#   VPS_HOST      — e.g. 217.76.56.66
#   VPS_SSH_KEY   — private SSH key (PEM) for deploy user
# Optional (staging auth — synced to /root/creator-ai-studio/.env.supabase.local):
#   VPS_USER                  — defaults to root
#   SUPABASE_URL              — e.g. https://iiokqyedkylwhonbrrvo.supabase.co
#   SUPABASE_ANON_KEY         — Dashboard → API → anon / publishable key
#   SUPABASE_JWT_SECRET       — Dashboard → API → JWT Secret
#   SUPABASE_SERVICE_ROLE_KEY — optional Postgres sync
#   CAS_API_KEY               — worker + machine-to-machine Bearer
#   GEMINI_API_KEY              — API Gemini provider (optional)
#   RCLONE_REMOTE               — e.g. gdrive:Creator-AI-Studio/episodes (optional)
#   RCLONE_OAUTH_TOKEN_JSON     — OAuth JSON for setup-rclone-vps workflow (optional, one-time)
#
# The VPS must have Docker, the Coolify app at /data/coolify/applications/...,
# and /root/creator-ai-studio as sync target.

name: Deploy Staging

on:
  push:
    branches: [staging]
  workflow_dispatch:

concurrency:
  group: deploy-staging
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Configure SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.VPS_SSH_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -H "${{ secrets.VPS_HOST }}" >> ~/.ssh/known_hosts

      - name: Sync code to VPS
        env:
          VPS_USER: ${{ secrets.VPS_USER || 'root' }}
          VPS_HOST: ${{ secrets.VPS_HOST }}
        run: |
          ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=yes \
            "${VPS_USER}@${VPS_HOST}" \
            "git config --global --add safe.directory /root/creator-ai-studio 2>/dev/null || true"
          rsync -avz \
            --exclude node_modules \
            --exclude .git \
            --exclude episodes \
            --exclude '.env*' \
            --exclude '*.enc' \
            -e "ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=yes" \
            ./ "${VPS_USER}@${VPS_HOST}:/root/creator-ai-studio/"

      - name: Verify Supabase secrets for web login build
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
          SUPABASE_JWT_SECRET: ${{ secrets.SUPABASE_JWT_SECRET }}
        run: |
          if [ -n "${SUPABASE_JWT_SECRET}" ] && { [ -z "${SUPABASE_URL}" ] || [ -z "${SUPABASE_ANON_KEY}" ]; }; then
            echo "::error::SUPABASE_JWT_SECRET is set but SUPABASE_URL and/or SUPABASE_ANON_KEY GitHub secrets are missing."
            echo "Add them under Settings → Secrets → Actions so the web image is built with VITE_SUPABASE_*."
            echo "See docs/02-operations/SUPABASE_AUTH.md"
            exit 1
          fi
          if [ -z "${SUPABASE_URL}" ] || [ -z "${SUPABASE_ANON_KEY}" ]; then
            echo "::warning::SUPABASE_URL or SUPABASE_ANON_KEY not in GitHub secrets."
            echo "Web login requires them at build time unless /root/creator-ai-studio/.env.supabase.local already exists on the VPS."
          fi

      - name: Sync Supabase env on VPS
        env:
          VPS_USER: ${{ secrets.VPS_USER || 'root' }}
          VPS_HOST: ${{ secrets.VPS_HOST }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
          SUPABASE_JWT_SECRET: ${{ secrets.SUPABASE_JWT_SECRET }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          CAS_API_KEY: ${{ secrets.CAS_API_KEY }}
          CAS_SECRETS_KEY: ${{ secrets.CAS_SECRETS_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          RCLONE_REMOTE: ${{ secrets.RCLONE_REMOTE }}
        run: |
          ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=yes \
            "${VPS_USER}@${VPS_HOST}" bash -s <<EOF
          SUPABASE_URL=${SUPABASE_URL@Q} \
          SUPABASE_ANON_KEY=${SUPABASE_ANON_KEY@Q} \
          SUPABASE_JWT_SECRET=${SUPABASE_JWT_SECRET@Q} \
          SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY@Q} \
          CAS_API_KEY=${CAS_API_KEY@Q} \
          CAS_SECRETS_KEY=${CAS_SECRETS_KEY@Q} \
          GEMINI_API_KEY=${GEMINI_API_KEY@Q} \
          RCLONE_REMOTE=${RCLONE_REMOTE@Q} \
          RCLONE_CONFIG=/config/rclone/rclone.conf \
          bash /root/creator-ai-studio/scripts/vps-sync-supabase-env.sh
          EOF

      - name: Redeploy staging stack
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          RCLONE_REMOTE: ${{ secrets.RCLONE_REMOTE }}
          VPS_USER: ${{ secrets.VPS_USER || 'root' }}
          VPS_HOST: ${{ secrets.VPS_HOST }}
          DEPLOY_TAG: e2e-pipeline-${{ github.sha }}
          COMPOSE_FILE: deploy/docker-compose.staging.yml
        run: |
          ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=yes \
            "${VPS_USER}@${VPS_HOST}" \
            "set -a; [ -f /root/creator-ai-studio/.env.supabase.local ] && . /root/creator-ai-studio/.env.supabase.local; set +a; COMPOSE_FILE=deploy/docker-compose.staging.yml bash /root/creator-ai-studio/scripts/vps-redeploy.sh ${DEPLOY_TAG}"

      - name: Auto-archive if over episode limit
        env:
          VPS_USER: ${{ secrets.VPS_USER || 'root' }}
          VPS_HOST: ${{ secrets.VPS_HOST }}
        run: |
          ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=yes \
            "${VPS_USER}@${VPS_HOST}" \
            "bash /root/creator-ai-studio/scripts/vps-archive-if-over-limit.sh || true"
