{
  "name": "creator-ai-studio",
  "version": "0.1.0",
  "private": true,
  "description": "Creator AI Studio — YouTube Christian Bible Channel Production System (executable MVP skeleton).",
  "workspaces": [
    "packages/*",
    "apps/*",
    "workers/*"
  ],
  "scripts": {
    "build": "npm run build --workspace @creator-ai-studio/shared && npm run build --workspace @creator-ai-studio/api && npm run build --workspace @creator-ai-studio/web && npm run build --workspace @creator-ai-studio/production-worker",
    "typecheck": "npm run build --workspace @creator-ai-studio/shared && npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "engines": {
    "node": ">=20"
  }
}
